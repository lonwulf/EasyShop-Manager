CREATE TABLE brand (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    origin TEXT
);

insertOrReplace:
    INSERT OR REPLACE INTO brand(id, name, origin)
    VALUES (?,?,?);

selectAllBrands:
    SELECT * FROM brand;

selectBrandById:
    SELECT * FROM brand WHERE id = :id;
